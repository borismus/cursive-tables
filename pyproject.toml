[project]
name = "cursive-tables"
version = "0.1.0"
description = "Schema-primed OCR and scoring pipeline for handwritten tabular archival records (Gemini-backed)"
requires-python = ">=3.10"
dependencies = [
    "google-genai",
    "python-dotenv",
    "jiwer",
    "scipy",
]
